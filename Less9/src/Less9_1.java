import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.io.InputStream;

import java.io.*;
import java.nio.file.Path;
import java.nio.file.Paths;

//public class Less9_1 {

    //public static void main(String[] args) throws IOException {
       // byte[] input = {1, 2, 4, 10, -4};
      //  byte[] output;
       // InputStream inputStream = new ByteArrayInputStream(input);
        /*OutputStream outputStream = new ObjectOutputStream(output);
        System.out.println(sumOfStream(inputStream, outputStream));
        //System.out.println(result);

    }

    public static void sumOfStream(InputStream inputStream, OutputStream outputStream) throws IOException {
        int a;
        int b;
        while ((a = inputStream.read()) != -1) {
            b = (byte) a;
            if (b % 2 == 0) {
                outputStream.write(b);
                outputStream.flush();
            }
        }


    }
}
*/
